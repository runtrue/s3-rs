[package]
name = "compare-rust-s3"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
rust-s3 = { version = "=0.37.2", default-features = false, features = ["tokio-rustls-tls"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
